<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="edu.pitt.cidde.portico"
    android:versionCode="1"
    android:versionName="1.0" >

    <uses-sdk
        android:minSdkVersion="10"
        android:targetSdkVersion="10" />

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.CALL_PHONE"/>

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/Theme.Blue" >
        <activity
            android:name="edu.pitt.cidde.portico.CiddeMainActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="edu.pitt.cidde.portico.CiddeHowToActivity"
            android:label="@string/app_name" >
        </activity>
        <activity
            android:name="edu.pitt.cidde.portico.ViewPagerActivity"
            android:label="@string/app_name" >
        </activity>

        <!-- David's additions -->
        <activity
            android:name="edu.pitt.cidde.portico.CiddeHelpActivity"
            android:label="@string/app_name" >
        </activity>

        <!-- Tim's additions -->
        <activity
            android:name="edu.pitt.cidde.portico.ClassRoomInfoActivity"
            android:label="@string/app_name" >
        </activity>
        <activity
            android:name="edu.pitt.cidde.portico.CiddeGPSActivity"
            android:label="@string/app_name" >
        </activity>
        <!--  -->

        <activity
            android:name="edu.pitt.cidde.portico.CiddeBrowseRooms"
            android:label="@string/app_name" >
        </activity>
        <!--  -->


        <!-- Eimile -->
        <activity android:name="CiddeScanQRcodeActivity" >
        </activity>
        <!--  -->
    </application>

</manifest>